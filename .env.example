# Copy to .env (gitignored). Never commit secrets.
# Generate APP_PASSWORD_HASH with: pnpm hash-password -- <password>
APP_PASSWORD_HASH=
APP_ORIGIN=http://localhost:5173
PORT=3000
DATABASE_PATH=data/app.sqlite
SESSION_DAYS=7
NODE_ENV=development
